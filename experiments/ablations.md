# Ablations

`train.py` implements baseline, learned decay with/without retention cost, random
decay, fixed decay, renormalized post-softmax decay, and pre-softmax bias.

It also evaluates zero-relevant, batch-shuffled, random, and all-one gate
interventions on learned variants.
